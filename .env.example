# Your Cloudinary cloud name (fallback is set in code)
CLOUDINARY_CLOUD_NAME=dwjnur7rd

# Optional: public URL to a JSON file mapping keys -> image URLs.
# If set, the app will fetch this JSON at startup; otherwise it will use the bundled assets/config/images.json
# Example: IMAGES_CONFIG_URL=https://raw.githubusercontent.com/youruser/yourrepo/main/images.json
IMAGES_CONFIG_URL=
